#!/usr/bin/env bash

function main {
  local shome="${_openvpn_home:-"$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"}"
  source "$shome/script/profile"

  if [[ -x "$(type -P openvpn)" ]]; then
    return 1
  else
    return 0
  fi
}

source sub "$0" "$@"
